server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Serve maintenance page for ALL requests
    location / {
        try_files /index.html =404;
    }
}
